node_modules/
dist/
target/
src-tauri/target/
src-tauri/target-updater-test/
*.log
scratch/

# Executaveis e instaladores locais de release
release/
Release/

# Build artifacts da extensão (gerados pelo web-ext / build.mjs).
# O firefox-extension.xpi é mantido de propósito: é embutido no binário do app
# via include_bytes! e precisa existir no repo para o build compilar.
browser-extension/dist-chromium/
browser-extension/dist-firefox/
browser-extension/release/*.xpi
!browser-extension/release/firefox-extension.xpi

# Backups e zips locais
*.zip
